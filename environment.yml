name: en-es-finetune
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytorch
  - pytorch-cuda=12.1
  - pip:
      - accelerate>=0.34.0
      - bitsandbytes>=0.43.3
      - datasets>=2.21.0
      - peft>=0.12.0
      - transformers>=4.55.0
      - tqdm>=4.66.0
